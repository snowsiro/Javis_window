# WhisperFlow (JARVIS) — Windows 핵심 의존성
# 설치: pip install -r requirements.txt

# --- 시스템 트레이 UI ---
pystray>=0.19.4
pillow>=10.0.0

# --- 음성 인식 (STT) ---
faster-whisper>=1.0.0

# --- 오디오 입출력 ---
sounddevice>=0.4.6
numpy>=1.24.0

# --- 전역 단축키 / 입력 자동화 ---
pynput>=1.7.6
pyperclip>=1.8.2
pyautogui>=0.9.54

# --- TTS (Windows SAPI5) ---
pyttsx3>=2.90

# --- 프로세스/시스템 유틸 ---
psutil>=5.9.0

# --- Windows 네이티브 (활성 창/화면 좌표) ---
pywin32>=306 ; sys_platform == "win32"

# --- WebSocket + JARVIS UI 서버 ---
websockets>=12.0

# --- 상시 청취(웨이크 워드 "Hey Jarvis") + VAD ---
openwakeword>=0.6.0
silero-vad>=5.1
onnxruntime>=1.16.0
torch>=2.0.0

# --- 데스크톱 알림 (선택 폴백) ---
win10toast>=0.9 ; sys_platform == "win32"
